import { menuData } from "../constants/menu_data";
import useMenuStore from "../store/menu_store";

export const storeMenus = () => {
  //console.log("storeMenus", menuData);
  //useMenuStore.setState({ menus: menuData });
};
